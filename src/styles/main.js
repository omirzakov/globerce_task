import { makeStyles } from "@material-ui/core";

export const useStylesMain = makeStyles(() => ({
    wrapper: {
        maxWidth: "950px",
        margin: "0 auto"
    },
    cardItem: {
        minHeight: 400
    },
    cardItemPost: {
        minHeight: 200
    }
}));
